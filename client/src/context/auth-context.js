import React from 'react';

export default React.createContext({
    // userId: null,
    // token: null,
    // username: null,
    // login: (userId, token, username) => {},
    // logout: () => {}
})